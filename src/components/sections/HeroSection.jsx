
import React from 'react';
import HeroCarousel from '../HeroCarousel';

const HeroSection = () => {
  return (
    <section id="home" className="section-dark-blue text-white">
      <HeroCarousel />
    </section>
  );
};

export default HeroSection;
